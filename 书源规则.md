# 书源规则

源听通过一份 JSON 描述一个站点：怎么搜索、怎么打开详情、怎么取章节、怎么拿到音频地址。在「书源管理」里粘贴、选文件或填 URL 导入。可以是单个对象，也可以是对象数组。

导入时会做结构校验。下面标「必填」的字段缺了会导入失败。

## 1. 顶层结构

```jsonc
{
  "site": { },          // 必填
  "search": { },        // 必填
  "detail": { },        // 可选；没有则直接用搜索结果里的字段
  "chapters": { },      // 必填
  "audio": { },         // 可选；章节里已是直链时可省略或 type=direct
  "variables": { },     // 可选
  "auth": { },          // 可选
  "discover": { },      // 可选
  "skip": { }           // 可选
}
```

每个网络段落（`search` / `detail` / `chapters` / `audio` / `auth.acquire`）共用同一套请求字段，见第 3 节。

## 2. 校验

| 检查 | 失败提示 |
| --- | --- |
| 有 `site`，且 `site.name`、`site.url` 非空 | `缺少 site 对象` / `site.name 不能为空` / `site.url 不能为空` |
| 有 `search`，且 `search.url`、`search.parser.type` 非空 | 对应字段错误 |
| 有 `chapters`，且 `chapters.parser.type` 非空 | `缺少 chapters 对象` / `chapters.parser.type 不能为空` |
| `audio` 同时写了 `parser` 与 `responseParse` | `audio 勿同时声明 parser 与 responseParse` |
| 正文出现 `{{chapterIndex}}` 却没写 `audio.chapterIndexBase` | 必须声明基线 |
| 正文出现 `{{token}}` 却没有 `auth` | 必须配置 auth |
| 有加解密时 `resolvePolicy` 为 `eager`，或 `cachePlayUrl` 为 `true` | 导入失败 |
| `auth` 存在但 `auth.acquire` 不是对象 | `auth.acquire 必须为对象` |
| 加解密算法、信封、密钥不合法 | 见第 11 节 |

`chapters.url` 可以为空：为空时回退到书籍的 `detailUrl`。

## 3. 请求段落

`search`、`detail`、`chapters`、`audio`、`auth.acquire` 都可以带这些字段。

| 字段 | 说明 |
| --- | --- |
| `url` | 请求地址，支持模板 |
| `urls` | 备用地址，失败后按顺序再试。可与 `url` 同时写 |
| `method` | `GET`（默认）或 `POST` |
| `headers` | 请求头。值里的 `{{...}}` 会先渲染 |
| `body` | 请求体模板。有内容才会发出 body |
| `bodyEncoding` | `plain`（默认，未写 Content-Type 时按表单）或 `raw`（按 `text/plain`） |
| `charset` | 响应解码：`utf-8`（默认）、`gbk`、`gb2312`、`gb18030`。UTF-8 BOM 会自动去掉 |
| `requestCrypto` | 先渲染 `body`，再加密后发送。见第 11 节 |
| `responseParse` | 先解密（可选），再按字段取值。见第 11 节 |
| `parser` | 不走加解密时的列表解析。见第 5 节 |

相对路径（封面、详情、章节音频）会按站点 `site.url` 或当前页地址补成绝对地址。

## 4. 模板

`{{名字}}` 会换成当前上下文里的值。未命中的占位符变成空字符串。也支持 `{{$.a.b}}`，按 JSONPath 在上下文上取值。

| 变量 | 何时有 |
| --- | --- |
| `keyword` | 搜索。默认做 URL 编码；`encodeKeyword: false` 或走分类映射时不编码 |
| `query` | 搜索。用户输入的原文，不编码 |
| `page` | 搜索页码，从 1 开始。URL 里没有 `{{page}}` 时不能翻页 |
| `bookId` | 详情、章节、音频。没有搜索字段时，用 `variables.bookId` 从地址里抽 |
| `detailUrl` | 书籍详情页地址 |
| `title` | 书名 |
| `chapterTitle` | 当前章标题（仅音频） |
| `chapterIndex` | 当前章序号。`chapterIndexBase` 为 `1` 时第一章是 1，否则与列表下标一致（从 0 起） |
| `url` | 章节上已有的音频地址（仅音频） |
| `token` | `auth` 取到的令牌 |
| `userAgent` | 引擎默认 UA；配置了 `auth` 时写入上下文 |
| `pageUrl` | 验证码提交时的页面地址 |
| `code` | 用户输入的验证码 |

自定义变量见第 9 节。`auth.cookie` 还可以注入日期和 cookie 值，见第 10 节。

## 5. 解析器 `parser`

```jsonc
{
  "type": "json",
  "listSelector": "$.items",
  "fields": { "title": "$.title" },
  "listFilter": { "field": "$.format", "equals": ["mp3"] },
  "stripExtension": true
}
```

`type`：

| 类型 | `listSelector` | 字段写法 |
| --- | --- | --- |
| `json` | JSONPath，结果必须是数组 | `$.title`，或条目上的字段名，或含 `{{ }}` 的模板 |
| `nuxt` | 如 `searchResults.list`，从页面 `__NUXT_DATA__` 里取列表 | 同 json |
| `html` / `rss` / `xml` | CSS 选择器 | `选择器 @属性`，见下 |
| `direct` | 不需要 | 字段值是上下文里的键名；不写 `fields` 时把上下文字符串键原样输出 |

HTML 字段：

- `a @text`：文本。`@html` / `@innerHtml` 取 HTML
- `a @href`、`img @src`，或任意属性名
- 选择器留空（`@text`）表示当前列表节点本身
- 尾缀 `##正则`：有捕获组时取第 1 组，否则取整段匹配。例：`p @text##作者：(.+)`
- 字段值含 `{{ }}` 时不当成选择器，按模板渲染

`listFilter` 只对 json / nuxt 生效。`equals`、`contains` 都不区分大小写。`stripExtension: true` 会去掉标题末尾不超过 4 个字符的扩展名（如 `.mp3`）。

JSONPath：必须以 `$` 开头。支持 `$.a.b`、`$.list[0]`、`$.list[*]`、`$.list[]`。

搜索、详情、章节请用这些字段名：

| 用途 | 字段 |
| --- | --- |
| 书籍 id | `bookId` |
| 标题 | `title` |
| 作者 | `author` |
| 播音 | `narrator` |
| 封面 | `coverUrl` |
| 详情 | `detailUrl` |
| 分类 | `category` |
| 集数 | `chapterTotal` |
| 章节音频 | `audioUrl` |
| 时长 / 大小 / 日期 | `duration`、`size`、`pubDate` |
| 简介 | `description`（仅详情） |

## 6. `site`

```jsonc
"site": {
  "name": "示例站",
  "url": "https://example.com",
  "version": 1,
  "author": "作者",
  "description": "说明"
}
```

`name`、`url` 必填。`url` 用来补全相对链接，也是按地址去重导入的依据。`version`、`author`、`description` 只作展示。

## 7. `search`

```jsonc
"search": {
  "url": "https://example.com/search?q={{keyword}}&p={{page}}",
  "charset": "utf-8",
  "headers": { "Accept-Language": "zh-CN" },
  "encodeKeyword": true,
  "keywordMap": { "玄幻": "1", "武侠": "2" },
  "keywordMapUrl": "https://example.com/category/{{keyword}}",
  "keywordMapMissUrl": "https://example.com/search?wd={{keyword}}",
  "parser": {
    "type": "html",
    "listSelector": "ul.list li",
    "fields": {
      "title": "a @text",
      "detailUrl": "a @href",
      "bookId": "a @href##/book/(\\d+)",
      "coverUrl": "img @src",
      "author": ".author @text"
    }
  }
}
```

`keywordMap` 把分类名换成栏目 id。命中后，或用户直接输入纯数字时，改走 `keywordMapUrl`，并且**不编码**关键字。声明了映射但没命中、输入也不是纯数字时，若写了 `keywordMapMissUrl` 就改走它。

### 验证码 `search.captcha`

响应正文里出现指定字符串时，弹出验证码，用户提交后再解析搜索结果。同一次搜索共用 Cookie。

```jsonc
"captcha": {
  "detectContains": ["请输入验证码"],
  "imageSelector": "img.captcha @src",
  "formActionSelector": "form @action",
  "submitMethod": "POST",
  "body": "code={{code}}&q={{query}}"
}
```

`detectContains` 必填，可以是字符串或字符串数组。响应里出现其中任一字符串才弹出验证码；不写则不会检测。`imageSelector` 从页面取图片地址，也可以直接写 `imageUrl`。两者都没有时不显示图片。`body` 是提交正文，用 `{{code}}` 表示用户输入；不写则提交空内容。`submitMethod` 默认 `POST`。`formActionSelector` 默认 `form @action`，取不到时提交到当前页。

## 8. `detail` 与 `chapters`

`detail` 可选。没有 `detail.url` 时，书架上的书直接用搜索结果里的标题、作者、封面。

`chapters` 必填 `parser`。`url` 为空时请求书籍详情页。

章节没有 `audioUrl` 时默认丢掉。如果 `audio` 自己有 `url`，或配置了加解密，允许空地址，引擎会记成 `lazy://...`，播放时再去取。

## 9. `variables`

从详情地址里抽出 `bookId`，供后面的 URL 使用。变量名写 `bookId`。

```jsonc
"variables": {
  "bookId": {
    "from": "detailUrl",
    "regex": "albums/(\\d+)",
    "capture": 1
  }
}
```

`from` 是上下文键，`regex` 是正则，`capture` 默认 `1`。抽不到时，引擎还会试 `/albums/123`、`/album/123`、`/123.html`。

## 10. `auth`

播放或拉章节前，若规则里出现 `{{token}}`，引擎会先请求 `auth.acquire`，按 `tokenField`（默认 `token`）取出字符串，缓存 `cacheTtlSec` 秒（默认 3600）。

```jsonc
"auth": {
  "id": "guest",
  "cacheTtlSec": 3600,
  "tokenField": "token",
  "acquire": {
    "url": "https://example.com/api/guest",
    "method": "POST",
    "body": "{}",
    "responseParse": {
      "mode": "json",
      "fields": { "token": "$.access_token" }
    }
  }
}
```

`id` 只用于缓存分区，默认 `guest`。令牌放进请求头时自己写模板，例如 `"authorization": "Bearer {{token}}"`。

### Cookie 自举 `auth.cookie`

不发 `acquire` 也能往上下文里放 cookie 值，供 `headers.Cookie` 使用。

```jsonc
"auth": {
  "cookie": {
    "valueTemplate": "sid={{stamp}}",
    "variables": {
      "stamp": { "source": "date", "format": "yyyyMMdd" },
      "sign": {
        "algorithm": "sm4-ecb-pkcs7",
        "plaintext": "{{userAgent}}|{{stamp}}",
        "encoding": "base64",
        "key": {
          "kdf": "sha256",
          "input": "prefix|{{stamp}}",
          "encoding": "hex",
          "takeHexChars": 32
        }
      }
    }
  }
}
```

- 必须写 `valueTemplate`。渲染结果放在 `{{cookieValue}}`，请求头里自己引用，例如 `"Cookie": "sid={{cookieValue}}"`
- `variables` 里的每个名字也会放进上下文，模板和请求头都能用
- `source: date` 用当前日期，`format` 为 `DateFormat` 样式，默认 `yyyyMMdd`
- `transform: parseIntRadix36` 把这段数字转成 36 进制
- `algorithm`：`sm4-ecb-pkcs7` 或 `sm4-ecb`。`encoding` 为 `base64`（默认）或 `hex`
- `key` 可以是字符串；也可以是 `{ "kdf": "sha256", "input": "...", "takeHexChars": 32 }`

## 11. 加解密

引擎只负责按书源里的声明去加密、发请求、解密。密钥和参数由书源作者自己填写，应用不附带任何站点密钥。

写在 `requestCrypto`（加密请求体）或 `responseParse.decrypt`（解密响应）上：

```jsonc
"decrypt": {
  "algorithm": "aes-256-gcm",
  "key": "由书源作者填写",
  "keyEncoding": "hex",
  "prefix": "nonce",
  "encoding": "base64"
}
```

| 字段 | 说明 |
| --- | --- |
| `algorithm` | 见下表。未列出的算法引擎没有实现，导入会失败 |
| `key` | 密钥。必填 |
| `keyEncoding` | `hex`（默认）、`base64`、`utf8` |
| `prefix` | 密文前面带什么。`nonce`（默认，有 IV 的算法）、`version+nonce`、`none`（密文本身不含 IV，此时要另写 `iv`） |
| `iv` / `nonce` | 固定 IV。不写则加密时随机生成，并按 `prefix` 放进密文 |
| `encoding` | 整段密文的编码：`hex`（默认）、`base64`、`raw` |
| `versionByte` | `prefix` 为 `version+nonce` 时写入的版本字节，默认 `1` |

| 算法 | 密钥长度 | nonce / IV |
| --- | --- | --- |
| `aes-128-gcm` | 16 字节 | 12 |
| `aes-256-gcm` | 32 字节 | 12 |
| `aes-128-cbc` | 16 字节 | 16 |
| `aes-256-cbc` | 32 字节 | 16 |
| `chacha20-poly1305` | 32 字节 | 12 |
| `xchacha20-poly1305` | 32 字节 | 24 |
| `sm4-ecb` | 16 字节 | 无 |

GCM / ChaCha 的密文末尾带 16 字节校验码。CBC、SM4 使用 PKCS#7 填充。

可选 `reverseCiphertextOnVersion`：版本字节等于该数字时，先把密文整段反转再解密。

请求侧：渲染 `body` → 加密 → 按 `encoding` 得到字符串再发送。

响应侧 `responseParse`：

```jsonc
"responseParse": {
  "mode": "json",
  "payloadPath": "$.payload",
  "decrypt": {
    "algorithm": "aes-256-gcm",
    "key": "由书源作者填写",
    "keyEncoding": "hex",
    "prefix": "version+nonce",
    "encoding": "hex"
  },
  "fields": { "token": "$.token" },
  "audioUrl": "$.play_url"
}
```

必须写 `payloadPath` 才能从 JSON 里取出密文字段；不写则整段响应当密文。解密失败但正文看起来已是 JSON 时，章节一类请求会退回明文。`mode` 为 `json` 或 `direct` 时用 JSONPath；其它值交给对应的 `parser`。`audioUrl`、`token` 可以写在 `responseParse` 上，不必放进 `fields`。

## 12. `audio`

两种拿播放地址的方式。

章节里的 `audioUrl` 已经是直链：

```jsonc
"audio": { "parser": { "type": "direct" } }
```

或省略 `audio`。播放器直接用该地址。

章节页里还要再解析一次（没有独立 `audio.url`、也没有加解密）：

```jsonc
"audio": {
  "parser": {
    "type": "html",
    "listSelector": "body",
    "fields": { "audioUrl": "audio @src" }
  }
}
```

引擎请求章节的 `audioUrl`，再用 `parser` 取出真正的地址。

独立接口（常用于签名、时效链接）：

```jsonc
"audio": {
  "resolvePolicy": "lazy",
  "chapterIndexBase": 1,
  "cachePlayUrl": false,
  "url": "https://example.com/api/play",
  "method": "POST",
  "headers": { "authorization": "Bearer {{token}}" },
  "body": "{\"id\":\"{{bookId}}\",\"idx\":{{chapterIndex}}}",
  "bodyEncoding": "raw",
  "requestCrypto": { },
  "responseParse": {
    "mode": "json",
    "audioUrl": "$.play_url"
  }
}
```

`resolvePolicy`：

- `lazy`：只在当前章要播或要下时再请求
- `eager`：可以提前解析。有加解密时不允许
- 不写：只要有 `requestCrypto`、`decrypt` 或 `cachePlayUrl: false`，就按 lazy

有加解密时不能 `cachePlayUrl: true`。

## 13. `discover`

发现页只看这份配置。不写时，会用「热门 / 完结 / 连载」等词去搜，搜不到的区块自动隐藏。

```jsonc
"discover": {
  "categories": ["玄幻", "武侠"],
  "categoryGroups": [
    { "name": "小说", "items": ["玄幻", "武侠"] }
  ],
  "hot": { "title": "热门推荐", "queries": ["玄幻", "武侠"] },
  "rank": { "title": "本周榜单", "query": "热门" },
  "news": { "title": "新书速递", "query": "最新" }
}
```

- `categories` 是发现页横滑分类。要分组时再写 `categoryGroups`，不写 `categories` 时用各组前几项当横滑
- `hot`、`rank`、`news` 用 `{ "title", "query" }` 或 `{ "title", "queries" }`。设为 `false` 关闭该区块
- 这些词会拿去走当前书源的搜索

## 14. `skip`

书源级片头片尾，单位秒。优先级：单本书设置 > 这里 > 设置页里的全局默认。

```jsonc
"skip": { "intro": 0, "outro": 0 }
```

## 15. 最小示例

```jsonc
{
  "site": {
    "name": "示例",
    "url": "https://example.com",
    "version": 1
  },
  "search": {
    "url": "https://example.com/search?q={{keyword}}&p={{page}}",
    "parser": {
      "type": "json",
      "listSelector": "$.items",
      "fields": {
        "bookId": "$.id",
        "title": "$.title",
        "author": "$.author",
        "coverUrl": "$.cover",
        "detailUrl": "https://example.com/book/{{id}}"
      }
    }
  },
  "chapters": {
    "url": "https://example.com/book/{{bookId}}",
    "parser": {
      "type": "html",
      "listSelector": "ul.chapters li",
      "fields": {
        "title": "a @text",
        "audioUrl": "a @href"
      },
      "stripExtension": true
    }
  },
  "audio": { "parser": { "type": "direct" } },
  "skip": { "intro": 0, "outro": 0 }
}
```
